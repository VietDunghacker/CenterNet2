_BASE_: "Base-SparseRCNN.yaml"
MODEL:
  WEIGHTS: "/gdrive/My Drive/data/r101_300pro_3x_model.pth"
  RESNETS:
    SCALE: 4
    WIDTH_PER_GROUP: 26
    DEPTH: 101
    DEFORM_ON_PER_STAGE: [False, True, True, True] # on Res4, Res5
    DEFORM_MODULATED: True
  PIXEL_STD: [57.375, 57.120, 58.395]
  FPN:
    NORM: 'GN'
  ROI_BOX_HEAD:
    NORM: 'GN'
  ROI_HEADS:
    SCORE_THRESH_TEST: 0.001
  SparseRCNN:
    ACTIVATION: 'silu'
    NUM_PROPOSALS: 10
    NUM_CLASSES: 22
TEST:
  EVAL_PERIOD: 200
  DETECTIONS_PER_IMAGE: 10
SOLVER:
  MAX_ITER: 20000
  CHECKPOINT_PERIOD: 200
  LR_SCHEDULER_NAME: "WarmupCosineLR"
  OPTIMIZER: "ADAMW"
  BASE_LR: 0.000025
  IMS_PER_BATCH: 14
  WARMUP_ITERS: 500
  RESET_ITER: True
  AMP:
    ENABLED: True
DATALOADER:
  ASPECT_RATIO_GROUPING: False
  NUM_WORKERS: 4
  SAMPLER_TRAIN: "ClassAwareSampler"
INPUT:
  FORMAT: RGB
  CUSTOM_AUG: EfficientDetResizeCrop
  MAX_SIZE_TEST: 800
  MAX_SIZE_TRAIN: 800
  TRAIN_SIZE: 800
  TEST_SIZE: 800
  SCALE_RANGE: [0.9, 1]
  TEST_INPUT_TYPE: 'square'
OUTPUT_DIR: "/content/ckpt"
VERSION: 2
SEED: -1